# Armbian userpatches library configuration
# Override library functions and settings

# Custom kernel patch directory
# KERNELPATCHDIR="q9s-rk3128"

# Toolchain settings
# SKIP_EXTERNAL_TOOLCHAINS="yes"

# Build optimizations
# USE_CCACHE="yes"

# Custom hooks (if needed)
# These functions can be defined here to customize the build process:
#
# user_config()     - Called after default config is loaded
# family_tweaks()   - Called before image is created
# family_tweaks_bsp() - Called for BSP (Board Support Package) customization
